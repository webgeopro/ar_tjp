<?php
/*<script type="text/javascript">
    var fileName;
    fileName = file.name.split('.');
    fileName = fileName[0].replace(' ', '-');
</script>
<div class="progressWrapper" id="_this_id">
    <div class="progressContainer">
        <a href="#" style="visibility:hidden"> </a>
        <div class="progressName">_file_name</div>
        <div class="progressBarInProgress"></div>
        <div class="progressBarStatus">&nbsp;</div>
        <div class="progressTitle">
            Title:
            <input type="text" class="inputTitle" value="_fileName" name="Filedata[title]" id="__this_id" style="width:280px">
        </div>
    </div>
</div>*/
/*var progressWrapper = document.createElement("div");
progressWrapper.className = "progressWrapper";
progressWrapper.id = file.id;
var progressElement = document.createElement("div");
progressElement.className = "progressContainer";
var progressCancel = document.createElement("a");
progressCancel.className = "progressCancel";
progressCancel.href = "#";
//progressCancel.style.visibility = "hidden";
progressCancel.appendChild(document.createTextNode(" "));
var progressText = document.createElement("div");
progressText.className = "progressName";
progressText.appendChild(document.createTextNode(file.name));
var progressBar = document.createElement("div");
progressBar.className = "progressBarInProgress";
var progressStatus = document.createElement("div");
progressStatus.className = "progressBarStatus";
progressStatus.innerHTML = "&nbsp;";
var progressTitle = document.createElement("div");
progressTitle.className = "progressTitle";
progressTitle.appendChild(document.createTextNode('Title: '));
var fileName; // Поле ввода названия пазла
fileName = file.name.split('.');
fileName = fileName[0].replace(' ', '-');
var progressInput = document.createElement("input"); // Input для componentUrl
progressInput.className = "inputTitle";
progressInput.value = fileName;
progressInput.name = 'Filedata[title]';
progressInput.id = file.id;
progressInput.style.width = '280px';

progressTitle.appendChild(progressInput);
progressElement.appendChild(progressCancel); // 0 {id для вставки из других методов}
progressElement.appendChild(progressText);   // 1
progressElement.appendChild(progressTitle);  // 2
progressElement.appendChild(progressStatus); // 3
progressElement.appendChild(progressBar);    // 4

progressWrapper.appendChild(progressElement);
document.getElementById(targetID).appendChild(progressWrapper);*/

//$(progressWrapper).appendTo('#'+targetID);
/*document.getElementById(targetID).appendChild(this.fileProgressWrapper);
this.height = this.fileProgressWrapper.offsetHeight;
this.setTimer(null);*/
